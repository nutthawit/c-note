#include <stdio.h>

int main() {
  int a; // Integer  
  unsigned int b; // Only store positive numbers.
  char c; // Character
  short d; // Short integer
  long e; // Long integer
  float f; // Floating point
  double g; // Double-precision
  bool h;
  return 0;
}
